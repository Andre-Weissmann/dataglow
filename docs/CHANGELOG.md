# DATAGLOW — Changelog

A running, one-line-per-change log of what shipped and when. Adding an entry here
is part of finishing a change, not a separate step — see the paper-trail section
in [`AGENTS.md`](../AGENTS.md).

Newest first. Each entry is a single line; link out to the PR where useful. This
log was started on 2026-07-08 and seeded from recent merged pull requests — it is
not a complete reconstruction of DATAGLOW's full history, only a forward-looking
record from roughly that point on.

## Unreleased

<!-- NEW-ENTRIES-BELOW: append new changelog lines directly under this line, do not edit existing entries below -->
- Wire the Gen 42 Guided Conversational Pack Builder into the Validate tab UI (the DOM follow-up the agent-module PR #89 deliberately deferred). New thin presenter `js/agents/conversational-pack-ui.js` exports a pure gate `shouldOfferPackBuilder({enabled, questions})` (true only when the flag is on AND validation produced at least one data-grounded question) and `mountConversationalPackBuilder(...)`, which renders a one-question-at-a-time card into a new `#pack-builder-wrap` host in the Validate tab HEADER AREA (never a modal) using existing CSS classes. It owns only presentation + flow state and delegates every rule/interpretation/resolution decision to the already-shipped agents (question-generator, uncertainty-resolver, pack-builder). Layout matches the spec: two EQUAL-weight primary buttons (both `btn btn-primary`, so the UI never nudges toward "accept") plus a visually lower-emphasis free-text fallback below them (smaller/muted type), with the mic rendered only when the separate `conversationalPackBuilderVoice` flag is on (it isn't). Accept/free-text → "✅ Got it: {plain restatement}" → next question or the running summary ("Here's everything I've learned so far:" + confirmed rules + `[Add another]`/`[I'm done — save my pack]`, with `[Add another]` omitted once no grounded findings remain). Uncertainty ("I don't know"/"not sure"/blank on a flagged finding) routes through the resolver and shows only its Step-D unified suggestion (the debate/steps are never surfaced); a second "I don't know" parks the finding rather than re-asking. Finalize → name → `[Save locally]`/`[Export to share]` reusing the existing community-pack register (`domain-physics.registerRuntimePack`) and browser-download paths; the finalize path runs inside the pack builder's `runWithNetworkDenied`, and this module names no network primitive. `js/app-shell/main.js`'s `runValidation()` now calls `renderConversationalPackBuilder(ds, results)`, which builds the data-grounded context (column stats from `computeDistributionFingerprint`, missingness findings) and mounts the card — but ONLY when `isEnabled('conversationalPackBuilder')`; with the flag off (its shipped default) it empties `#pack-builder-body` and hides the host, so the feature ships DARK and nothing renders. New real-browser Playwright test `test/pack-builder-ui.test.mjs` (`npm run test:e2e-packbuilder-ui`, added to the `e2e-smoke` CI job; engine-independent so robust in headless CI): asserts the gate, that the flag-off path mounts nothing (regression guard), and the full flag-on flow (question card renders, equal-weight buttons, mic hidden, "✅ Got it", running summary lists a confirmed rule, done→name→finalize→Save/Export, plus the "not sure"→Step-D-resolution path with no debate leaked). No flag flipped, no voice/STT wired. Why: turn the merged-but-dark Gen 42 agent modules into a feature a non-technical domain expert can actually complete end-to-end in the Validate tab, without changing the zero-upload posture.
- Add the Gen 42 Guided Conversational Pack Builder: the cold-start fix for pack authoring, replacing the blank text box with a guided, data-grounded conversation a non-technical domain expert completes with one tap — no code. Four pure, browser-free, LLM-injected, network-clean agent modules. **Part 1 — data-grounded question generator** (`js/agents/question-generator-agent.js`): after the 20-layer pipeline runs, scans findings for the most "askable" anomalies in the spec's priority order (mathematically impossible values → extreme outliers → missingness clusters → format drift) and fills the fixed template `"I noticed your \`{column}\` column has {observation}. Is that expected, or should {ruleGuess}?"`, ALWAYS embedding a real observed value — `buildQuestion` refuses a non-grounded (generic) candidate. `buildQuestionView` returns the two equal-weight buttons (`Sounds right — use that` / `Skip for now`) plus a LOW-EMPHASIS free-text/voice fallback (progressive disclosure, not a third button); `ghostCompletion` does peer/common-pattern inline autocomplete; `buildQuestionPrompt` optionally polishes wording on-device without inventing a value. **Part 2 — "I don't know" resolution engine** (`js/agents/uncertainty-resolver-agent.js`): on uncertainty, resolves entirely on-device in a fixed order — Step A statistical-confidence check (hard-constraint + >3SD + rare → confident), Step B local peer-index borrow, Step C a three-agent debate panel (Conservative/Industry-Norm/Statistical) run SEQUENTIALLY against one WebGPU context with confidence-weighted reconciliation (not majority vote) under a 2-second budget that falls back to a safe default when blown, Step D ONE unified suggestion (the debate is never shown), Step E park-and-revisit that stops re-asking on the second "I don't know" and only revisits with new cross-column evidence. Degrades to deterministic per-role proposals with no LLM. **Part 3 — local peer-sourced pack index** (`js/packs/local-pack-index.js`): a flat, content-addressed (`sha256:<64 hex>`), read-only index fetched once via an INJECTED fetcher (so the module names no network primitive and passes the `js/packs/` scan) and served thereafter from the `sw.js` cache; validates and drops malformed entries, answers pure synchronous Step-B lookups, and degrades to an empty index offline. Never auto-applies a peer rule. **Part 4 — guided pack builder** (`js/agents/pack-builder-agent.js`): consumes CONFIRMED answers however they arrived (button / typed / on-device voice transcription / resolver acceptance — all parsed identically), shows the running summary "Here's everything I've learned so far:" with `[Add another]` / `[I'm done — save my pack]`, and `finalize` assembles a portable envelope, validates it through the EXISTING `js/teaching/community-pack.js` schema (`validateImportedPack`/`importPack`) and proves it carries no network code via the pack no-network guard (`assertNoNetwork` inside `runWithNetworkDenied`), then offers `[Save locally]` / `[Export to share]`. EMPOWERMENT CONSTRAINT honoured throughout: a rule enters a pack only after explicit user confirmation — no module ever writes an inferred rule on its own. Ships behind `conversationalPackBuilder` (agent modules land dark; Validate-tab DOM wiring is a follow-up PR) and `conversationalPackBuilderVoice` (typed path fully works; the mic is gated pending a vendored permissively-licensed on-device WASM/WebGPU STT model — voice is just transcribed text through the same path). SCOPE JUDGMENT CALL (flagged): the portable community-pack vocabulary is annotate-only (no-merge / benford-exempt / outlier-context) and has no hard-fail "bound check" kind, so a learned bound ("discounts never exceed 100%") is captured as the closest existing kind — an `outlier-context` rule whose plain reason records the bound — keeping the built pack inside the proven sandbox; a real bound-check rule kind would require extending `domain-physics.js` + the portable schema and is deliberately out of scope. Tests (`npm run test:questiongen`, `test:uncertainty`, `test:packindex`, `test:packbuilder`, run by the new `conversational-pack-builder` CI job) assert questions reference real values (never generic), Steps A–E run in order with the 2-second budget and zero network calls, the peer index is read-only and offline-safe, and five sample sessions (including voice-transcribed and typed) each build a schema-valid pack. Why: give non-technical domain experts a way to teach their expertise as validation rules through a conversation grounded in their own data, without ever surrendering the final say to the AI.
- Fix three confirmed bugs found by hands-on browser testing of the Clean, Diff, Visualize, Story, and Digital Twin tabs, bundled in one pass. **Bug 1 (Clean tab — silent crash on text columns):** `js/cleaning/clean.js` offered the numeric-only null fixes "Fill with mean" and "Fill with 0" for *every* nulled column, including `VARCHAR` ones; clicking them ran `AVG(VARCHAR)` (a DuckDB binder error) or wrote `0` into text, and because the Clean click handler has no error surface the failure was silent. `scanForIssues` now type-gates the offered fixes — numeric columns keep all four (`drop_rows`/`fill_zero`/`fill_mean`/`fill_mode`), text columns get only the type-safe `drop_rows`/`fill_mode`. **Bug 2 (protocol conformance — schema 404s):** `js/protocol/protocol-conformance.js` fetched JSON schemas from `../protocol/schema/` which, resolved against the module at `js/protocol/`, pointed at the non-existent `js/protocol/schema/` and 404-ed every dev-context (`localhost`/`?protocolCheck`) conformance check; corrected to `../../protocol/schema/` so it matches the sibling `../../protocol/validator.mjs` import and resolves to the real repo-root `protocol/schema/`. **Bug 3 (Narrative Consistency — false mismatches):** the Story tab formats figures with `toLocaleString` (e.g. `1,000,062.09`) and appends a confidence badge reading `n=<count> · <pct>% missing`, but `checkNarrativeConsistency` in `js/validation/validation.js` (a) split grouped numbers on their thousands separators into fragment "mismatches" and (b) never registered the per-column non-null count or missing-rate percentage — so correct narratives were flagged as inconsistent; the number extractor is now grouping-aware (strips separators before comparison) and the per-column `n`/missing% are added to the accepted-number set. Regression tests: bug 3 (grouping separators + badge metadata, with a wrong-figure negative control) in `test/batched-bugfixes-layers.test.mjs` (`npm run test:bugfixes-layers`); bug 2 (schema-URL depth guard) in `test/protocol-schema.test.mjs` (`npm run test:protocol`, newly wired into the `batched-bugfixes` CI job, where it previously ran nowhere); bug 1 verified in a real headless browser (text-column nulls offer only type-safe fixes and applying one throws nothing). Why: each bug either silently broke a user action (Clean crash), spammed the dev console with 404s and disabled the conformance check, or told the user their own correct data story was inconsistent.
- Fix four independent, previously-confirmed bugs in one pass, each guarded by a fail-first regression test (verified to fail against pre-fix code and pass after) run by the new `batched-bugfixes` CI job. **Bug 1 (provenance):** DuckDB-WASM's `registerFileBuffer` transfers/detaches the `ArrayBuffer` it is handed, and the file-load path hashes those same raw bytes for the Chain of Custody — so if the engine detached the caller's buffer the later hash threw and the audit trail silently recorded nothing. `js/app-shell/duckdb-engine.js` now hands the engine an independent copy via a new exported `duckdbBytes()` (`.slice()`), decoupling the engine's transferable buffer from the caller's bytes so hashing (and any retry/re-read) stays valid regardless of call order. **Bug 2 (Benford's Law):** the CORE eligibility gate in `js/validation/validation.js` now recognises pure 0/1 flag columns itself (skip cause `binary_flag`) rather than relying on a domain pack's binary rule to relabel them — previously, with `pack:'none'`, a boolean flag fell through to the generic `narrow_range`/`small_sample` skip. **Bug 3 (Narrative Consistency):** a proportion column (every value in [0,1]) narrated as a PERCENTAGE (mean 0.30 → "30.0%") is now accepted instead of flagged as a false mismatch, by adding the ×100 percentage forms of the mean/min/max to the accepted-number set. **Bug 4 (SQL editor):** the plain textarea gains lightweight, dependency-free, zero-network syntax highlighting — a new pure, Node-testable tokenizer (`js/app-shell/sql-highlight.js`) paints a `<pre>` overlay behind a transparent-text textarea (`tok-keyword`/`tok-string`/`tok-comment`/`tok-number`/`tok-function` spans, HTML-escaped) — plus structured DuckDB error formatting (kind / detail / actionable hint) replacing the raw error dump. CodeMirror/Monaco were rejected: both would mean a CDN runtime dependency (forbidden for the zero-upload core app) or vendoring a bundle that needs a build step this project intentionally omits. Tests: `npm run test:bugfixes-layers` (native DuckDB, Bugs 2–3) and `npm run test:bugfixes-editor` (plain Node, Bugs 1 & 4). Why: guarantee the provenance audit trail is never silently empty, stop two false/again-crash validation findings, and make the SQL tab readable without breaking the local-first, no-build constraints.
- Fix four confirmed bugs in shipped code, each written test-first (regression test proven to fail against current code before the fix, pass after) and bundled in one PR. **Bug 1 — silent 200K-row truncation in the Python bridge:** `js/runtimes-viz/python-runtime.js` capped every dataset at `LIMIT 200000` before re-serializing to pandas with no user warning; now exports `PY_BRIDGE_ROW_LIMIT` + a pure `computeBridgeTruncation()` and `runPython` returns a `truncated` descriptor that `js/app-shell/main.js` renders as a visible, dismissible banner (`data-testid="py-truncation-warning"`) stating the data was truncated to N rows for Python operations (the limit stays; the silence was the bug). `npm run test:pytruncation`. **Bug 2 — CSV `IGNORE_ERRORS=TRUE` silently dropped malformed rows:** `js/app-shell/duckdb-engine.js` now loads via `store_rejects=true`/`rejects_table`/`rejects_scan` and counts dropped rows (`COUNT(DISTINCT line)`), `createTableFromCSV` returns `{droppedRows}`, and `js/app-shell/loaders.js` surfaces a "Loaded X of Y rows — Z skipped due to parsing errors" warn toast plus a per-dataset `⚠ N skipped` sidebar badge. `npm run test:csvrejects`. **Bug 3 — SQLite advertised on the upload UI but rejected at runtime:** removed `.sqlite`/`.db` from the file-input `accept` list and the caption in `index.html` (fix (a): only advertise formats that actually load), and made the SQLite loader message honest. `npm run test:uploadformats`. **Bug 4 — CDN-runtime scripts undercut the "fully offline" claim (judgment call):** did NOT vendor Pyodide/WebR/WebLLM (each is a large runtime that would bloat every page load and violate the size/perf + no-new-page-load-dependency constraints; Plotly & SheetJS were already vendored, contrary to the ticket premise); instead made the claims precise — one-time-download disclosures on the Python and R panels and a canonical `data-testid="offline-capability-note"` in the About section that separates the self-hosted core (DuckDB/Plotly/SheetJS, offline from first load) from the on-demand runtimes (Pyodide/WebR/WebLLM), matching the README split. `npm run test:offlineclaims`. All four run in the new `shipped-bug-regressions` CI job (`.github/workflows/job-shipped-bug-regressions.yml`). Why: each bug silently misled users about what the app actually did with their data or environment; surfacing the truth (or removing the false claim) is cheaper and safer than the silent behaviour.
- Close a pre-isolation load race found via independent verification of the COOP/COEP fix: on hosts that fall back to the `sw.js` header-injection path (the live pplx.app deployment does not honor `_headers`), clicking "Load Golden Test Dataset" or dropping a file during the brief window before the one-time service-worker reload tore the click handler's execution context down mid-flight, silently dropping the load with no data, no error, and no toast — reproducing the exact original silent-failure symptom. `index.html` now publishes `window.__dataglowIsolation` (`isolated`/`pending`/`failed`/`unsupported`); the sample-dataset buttons route through `requestDatasetLoad(id)` in `js/app-shell/main.js`, which — while the page is still `pending` — persists the request (`dataglow-pending-load`) and shows a non-error "Starting the data engine…" state instead of starting a doomed load, then `replayPendingDatasetLoad()` automatically replays it after the reload lands on the isolated page (file uploads, which can't survive a reload, show the same "starting" state so the user re-adds the file moments later). The background pre-warm likewise shows the transient "starting" state rather than flashing a scary error during that known-transient window. New real-browser regression test `test/coi-race.e2e.test.mjs` (`npm run test:e2e-coi-race`, added to the e2e-smoke CI job) deterministically recreates the race by delaying the `sw.js` fetch and asserts a fast click is queued and replayed — never silently dropped; the static `test:coi` suite gains matching assertions for the new guard. Why: production lands on the SW-fallback path, so the race was likely to hit real first-time visitors exactly as the original bug did.
- Fix a critical production bug where the live app could not load ANY dataset (Golden sample, file upload, or the internal self-test) because the page was not cross-origin isolated — `window.crossOriginIsolated` was `false`, `SharedArrayBuffer` was unavailable, and the DuckDB-WASM engine silently failed to initialize, leaving the UI stuck on "No dataset loaded" with zero error. Deliver the required COOP/COEP headers two ways: a host-level `_headers` file (`Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: credentialless`, plus `application/wasm` for the DuckDB binaries) and a host-agnostic fallback in `sw.js` that injects the same headers on every same-origin response it serves (a `withCrossOriginIsolation` wrapper on the navigation + subresource paths); `index.html` reloads once via a loop-guarded `controllerchange` handler so a first visit becomes isolated as soon as the service worker takes control. COEP is `credentialless` (not `require-corp`) so the opt-in cross-origin CDN runtimes (Pyodide/WebR/WebLLM) and Google Fonts still load without per-asset CORP/CORS. Independently, fix the silent-failure UX (defense-in-depth so a future engine regression can never fail invisibly again): `js/app-shell/main.js` now surfaces a visible, retryable "Couldn't start the data engine" banner with the real failure reason (and a cross-origin-isolation hint when that's the cause) whenever the engine fails to start or a dataset fails to load — the Golden/OMOP/FHIR buttons, file drops, the Red Team self-test, and the background engine pre-warm all route through it instead of rejecting silently. New `npm run test:coi` (`test/coi-headers.test.mjs`, `coi-headers` CI job) keeps both header-delivery paths and the UX guard honest without a browser. Why: the published app was fully broken for every first-time visitor, and the failure was completely silent.
- Add the Gen 40 Domain-Pack Plugin Architecture: convert the domain packs (`none`, `healthcare`, `retail`, `finance`, plus the OMOP and FHIR healthcare profiles) from code pasted into the single shared `js/validation/domain-physics.js` file into self-contained, manifest-declared plugins under a new `js/packs/` tree, so adding/updating/removing a pack is a new file plus one registration line and two packs never collide on the same file again. This is a pure architectural refactor — behaviour is assertion-for-assertion identical (the plugin modules reference the *same* runtime pack objects, proven by a legacy-vs-plugin identical-output test per extension point). New `js/packs/extension-points.js` names the closed vocabulary of six stable extension points (validation-rules, cleaning-fixes, anomaly-detectors, teaching-notes, sample-datasets, vocabulary); a pack manifest declares which points it fills and the loader rejects any unknown point. New `js/packs/pack-network-guard.js` makes the zero-upload guarantee enforced rather than conventional: a static source scan (`scanSourceForNetwork`/`assertNoNetwork`) rejects any pack whose code references a network primitive (`fetch`, `XMLHttpRequest`, `WebSocket`, …) and a runtime trap (`runWithNetworkDenied`) shadows those globals as defence in depth — the new `pack-architecture` test includes a test-only rogue pack that tries to `fetch()` and passes only because the guard catches it, and it scans every shipped pack file. New `js/packs/pack-registry.js` (`loadBuiltInPacks`) validates each manifest (id↔pack.name, semver version, industry, capabilities ⊆ extension points, and rejects any inter-pack dependency so packs stay isolated) and assembles the `{id → pack}` map the engine installs. `js/validation/domain-physics.js` gains an additive swappable pack-source seam (`setPackSource`/`getPackByName`/`registerRuntimePack`); the migration lands behind a `pluginPacks` flag in the existing `flags.manifest.json` (read by `js/build/build-flags.js`) wired into `js/app-shell/main.js`'s capability bootstrap, so it can be rolled back by flipping one flag. The Validate tab now surfaces a "Loaded domain packs" audit disclosure (id / version / extension points / sample-data license & provenance), mirrored in `TRUST.md`. Registered as the `domain-pack-plugins` capability in `capability-map.manifest.json` (with `docs/capability-map.md`), run in CI by the new `pack-architecture` job (`npm run test:packs`). Why: make each domain pack an independent, auditable, offline-guaranteed unit so the pack surface can grow without packs stepping on each other or the core engine.
- Complete the Context Engine (Gen 34 Stage C + D) with two additive, dependency-free features. **Stage C — Teach As You Clean** (`js/teaching/micro-lessons.js`): an optional one-line "why this matters" micro-explanation attached to every validation finding — all 20 layers plus the Red Team self-test, each domain-pack reinterpretation (healthcare/retail/finance), and the finer Unit Test (negative/future-date/blank-key/duplicate/null-ref) and Benford skip-cause sub-findings. A "Learn while you clean" toggle (on by default) shows/hides all of them, and a Beginner/Practitioner/Expert verbosity slider changes only the wording register — never which findings appear, their severity, or any validation logic. All explanation copy is original one-sentence wording. State is session-only, read from the DOM each render (mirroring the Context Card) — no localStorage/sessionStorage/IndexedDB/cookies. A coverage test (`npm run test:microlessons`) asserts every live `LAYER_DEFS` id and every `DOMAIN_PACKS` rule id has a lesson, so a new layer/rule can't ship without one. **Stage D — Community Pack Sharing** (`js/teaching/community-pack.js`): export a descriptor-based domain pack to portable JSON and import a shared one back, reusing the app's existing browser download/upload — no server, marketplace, or backend. To make packs portable, `js/validation/domain-physics.js` now expresses the built-in Retail and Finance packs as declarative descriptors compiled through three exported annotate-only factories (`makeNoMergeRule`/`makeBinaryBenfordExemptRule`/`makeOutlierContextRule`) via a single `compilePackRule`/`compileColumnMatch` path; behavior is byte-for-byte identical (Stage B tests unchanged). An imported pack is validated against a strict, closed schema (`validateImportedPack`: rejects unknown keys, wrong envelope kind/version, reserved names, empty/oversized rules, disallowed stateful regex flags, uncompilable/oversized patterns, duplicate ids) and then compiled through that same factory path — the schema IS the sandbox: each rule's target layer is derived from its `kind` (never taken from input), so an imported pack can only ever annotate/reinterpret findings and can never hard-fail data, auto-merge protected categories, or touch a core layer like `unit_tests`. `npm run test:communitypack` covers round-trip export→import for retail/finance, valid hand-authored imports, and rejection of malformed/malicious-shaped envelopes. Limitation (also noted in the PR): the hand-written healthcare pack uses bespoke predicates (`isSensitiveCategory`, wholesale-date-shift detection) that aren't factory-expressible, so export is scoped to the descriptor-based packs (retail, finance, and any imported pack); the healthcare and empty `none` packs are non-portable and report a clear reason. Both modules are registered in `capability-map.manifest.json` (`platforms: ["browser","desktop"]`) and reached via `registry.get('micro-lessons')`/`registry.get('community-pack')`; UI lives in the Validate tab header in `js/app-shell/main.js`. Why: teach users the reasoning behind each check as they clean, and let a team share its domain reinterpretations as a plain file without any hosted infrastructure or new trust surface.
- Add The Standards Bridge (Gen 33): recognise two common healthcare-data standards — the OMOP Common Data Model and HL7 FHIR bundles — and route them through DATAGLOW's existing validation engines, adding no new validation math or ML. A new pure module `js/validation/health-standards.js` recognises five in-scope OMOP tables (PERSON, CONDITION_OCCURRENCE, DRUG_EXPOSURE, MEASUREMENT, OBSERVATION_PERIOD) and four FHIR resources (Patient, Condition, Observation, Encounter), maps their long-format concepts (e.g. `measurement_concept_id` / FHIR Observation codes) onto the tabular shape the layers expect, and reuses the Physiological Plausibility layer's `VITALS` bounds and the Missingness Detective's threshold verbatim. It implements the specified violations — OMOP observation-period end-before-start and condition-outside-lifespan (cross-column), implausible `value_as_number` (physiological plausibility); FHIR out-of-bounds `valueQuantity` (physiological), birthDate-vs-Encounter.age inconsistency (cross-column), and missingness across encounters. Two new swappable Domain Packs (`omop`, `fhir`) are registered in `js/validation/domain-physics.js` following the existing Retail/Finance factory pattern; as clinical data they reuse the healthcare pack's reinterpretations unchanged and additionally carry a shared non-clinical medical disclaimer ("DATAGLOW is a data-quality reasoning assistant, not a medical or clinical AI") surfaced in the Validate tab wherever their findings appear. Stage A ships small, entirely synthetic OMOP/FHIR sample fixtures (never real PHI, inspired by Synthea's public description — copied from no upstream file) with a few planted issues, loadable via two new "Load OMOP CDM Sample" / "Load FHIR Bundle Sample" buttons that reuse the existing `loadRowsAsDataset` ingestion path and auto-select the matching pack. `validation.js` core logic is untouched; all three stages are pure JS with no browser-only API, so they inherit across browser + desktop like the other packs. New unit suite `npm run test:standards` (wired into the SQL-logic CI job) covers schema recognition, concept mapping, and every listed violation. Why: let analysts validate standards-shaped healthcare data without hand-reshaping it first, while keeping DATAGLOW strictly a data-quality tool.
- Generalize the Domain Pack Marketplace beyond healthcare and add an optional Context Card (Gen 34 Stage A + B). `js/validation/domain-physics.js` now ships two new swappable packs alongside `none` and `healthcare`: **Retail / E-commerce** (SKU/product-code merge guards, binary return/refund-flag Benford exemptions, seasonal/promotional outlier reinterpretation) and **Finance / Accounting** (ledger/GL-account merge guards, binary reconciliation-flag Benford exemptions, offsetting debit/credit outlier reinterpretation). Each new rule follows the existing pack rule shape and obeys the safety contract — heuristic-only, annotate via `ctx.annotations`, never delete a finding silently or introduce a new hard failure, and any thrown rule is caught by the engine and skipped rather than breaking the run. The healthcare pack's three existing rules are unchanged, and the ~20 core validation layers in `validation.js` are untouched (packs still sit strictly above them). The Validate tab's pack picker (driven by `listPacks()`) now shows all four packs. A new optional **Context Card** ("What is this data for?") feeds the Problem Framer's new `orderLayersByContext` (`js/problem-framing/problem-framer.js`), which re-orders the validation grid so the layers most relevant to the stated context surface first (e.g. "billing accuracy" floats numeric/financial layers ahead of formatting checks); it is display-order only — every layer still runs — and skipping it leaves ordering identical to before. Session-only, nothing stored or uploaded; identical in the browser and Tauri desktop shell. Why: move DATAGLOW beyond healthcare-only framing so the swappable-pack architecture serves retail and finance data too, and let users cheaply tell the tool what matters most without any new persistence. (Stage C micro-lessons and Stage D community pack export/import are intentionally out of scope for this slice.)
- Add a human-facing `TRUST.md` repo-trust page and link it prominently near the top of `README.md`. It carries a first-party, verifiable Repo Health section (per-job CI split, the existing CycloneDX SBOM generated every run by `job-supply-chain-hardening.yml`, the CI Provenance Ledger + `npm run verify:ci-provenance`, and a "see latest commit date" last-verified convention — no invented numbers), an original-wording "How This Repo Talks to Agents" explainer of how `AGENTS.md`, the capability map, and the append-only changelog work together, and a "Start Here" section linking three curated `good-first-task` issues (#57, #46, #50). Also extend `.github/PULL_REQUEST_TEMPLATE.md` with What changed / Why / What was tested / (optional) What was considered and rejected sections while preserving the Build Nervous System intent/gen/integrate record, and create the `good-first-task` label. Docs/repo-surface only — no `js/`, no product behavior, no CI-logic change. Why: give an outside human (recruiter, contributor, portfolio reviewer) an honest, inspectable, first-party alternative to third-party repo-trust scores.
- Add a CI Provenance Ledger: a self-contained, tamper-evident SHA-256 hash chain over CI outputs plus a zero-dependency offline verifier (the lightweight alternative to SLSA hosted attestation that an earlier design pass recommended but never built). A new standalone workflow `.github/workflows/ci-provenance-ledger.yml` fires on completion of the `tests` workflow, filtered to `main` landings only (never PR branches), and appends one line to the append-only `docs/ci-provenance-ledger.jsonl` recording `commit`, `timestamp` (ISO 8601 UTC), `test_conclusion` (the real conclusion of that test run), `sbom_hash` (SHA-256 of the SBOM produced by the existing `npm run sbom` — reused, not duplicated), `prev_hash` (previous entry's hash, or a 64-zero genesis anchor), and `entry_hash` (SHA-256 of this entry's own contents). It commits back via the same carrier-branch self-PR + `[skip ci]` loop-guard pattern as `living-manifest.yml`. `npm run verify:ci-provenance` (`.github/scripts/verify-ci-provenance.mjs`) recomputes and re-links every entry fully offline (zero network, zero GitHub API), printing "N entries verified, chain intact" or naming the exact entry that broke; the appender (`.github/scripts/append-ci-ledger.mjs`) and verifier share one canonical hashing helper (`.github/scripts/ci-ledger-hash.mjs`) so the writer and checker can never disagree. Also adds a short honest "Supply-chain" section/badge to `README.md` (SBOM every run, zero unreviewed install scripts, ledger + verifier links; described only as roughly SLSA L1 — no higher claim, no attestation/signing service added). Recording only — human-on-the-loop, nothing auto-fixes CI or edits app code. Why: give a solo-maintained repo an independently verifiable, offline build-provenance record without the complexity of a hosted attestation service.
- Repo hygiene: rename the three generation-numbered test files to descriptive names — `test/gen8-batch3.test.mjs` → `test/trust-collaboration-suite.test.mjs`, `test/gen8-features.test.mjs` → `test/trust-adversarial-suite.test.mjs`, `test/gen9-batch3.test.mjs` → `test/synthetic-twin-time-machine-suite.test.mjs` (via `git mv`, history preserved), updating their `RUN WITH` header comments and the `test:gen8`/`test:gen8b`/`test:gen9b` script paths in `package.json`. Also add `docs/DECISIONS.md`, a one-page index of the project's design-decision history (a generation/theme table marking each decision Built vs. external-record-only). Docs/test-naming only — no `js/` or product behavior change. Why: opaque gen-batch filenames obscured what each suite covers, and there was no single navigable map of why the project is shaped the way it is. a four-stage build-safety spine documented in `docs/build-nervous-system.md`. (1) Isolate: a git-worktree convention + helper `scripts/new-agent-worktree.sh <branch>` so each coding-agent session gets its own working directory. (2) Author: a three-layer `intent`/`gen`/`integrate` commit/PR convention recorded in `AGENTS.md` and required by a new `.github/PULL_REQUEST_TEMPLATE.md`. (3) Gate: a `.github/workflows/merge-tree-preflight.yml` workflow that runs `git merge-tree` on every PR and fails (as a regular, non-required check) if merging into current `main` would textually conflict — a pure in-memory simulation that never merges or pushes — plus a new `titanic-validation-layers` golden regression case (`test/golden/cases.mjs` + fixture) pinning the full validation orchestrator's verdicts on a Titanic passenger-manifest shape. (4) Land dark: a root `flags.manifest.json` (flag -> `{enabled, addedInPR, description}`) read by `js/build/build-flags.js` (`isEnabled(name)`; in-memory only, no localStorage/cookies/network, identical in browser, Tauri desktop, and future Tauri mobile) with one disabled `exampleFlag`, plus a promote-or-delete flag-hygiene rule. Additive only: no branch-protection or required-check changes, and no existing `js/` behavior is wired to the flag helper. Why: replace ad-hoc build-safety habits with one coherent, buildable spine that isolates sessions, records intent, gates merges, and lets risky behavior ship dark.
- Add an Export / Reporting layer that turns the loaded, validated dataset into a downloadable Excel workbook or PDF report, 100% client-side. A Universal Export Contract (`js/export/export-report.js`) builds raw bytes per format decoupled from delivery: the `.xlsx` path reuses the already-vendored SheetJS (`assets/xlsx/`, no new dependency) and emits a Data sheet plus a Summary sheet and — when validation metadata is present — a Validation Summary sheet; the PDF path is a small first-party, dependency-free PDF 1.4 writer (catalog/pages/font objects + byte-offset xref table, ASCII-safe text) carrying the title, generation timestamp, row/column counts and validation summary. Delivery is handled by small platform adapters (`js/export/export-delivery.js`): the browser adapter uses the repo's existing Blob + object-URL + synthetic `<a download>` pattern; the desktop adapter feature-detects the Tauri `dialog.save`/`fs.writeBinaryFile` APIs for a native Save-As and transparently falls back to the browser download when they're absent (the shell's current deny-by-default posture), and reports a clean not-delivered/cancelled result when the user cancels the dialog; the mobile adapter is an explicit future-work share-sheet stub that throws. The module is registry-native (`export-reporting` capability in `capability-map.manifest.json`, `platforms: ["browser","desktop"]`) and reached from `js/app-shell/main.js` via `registry.get('export-report')`; UI entry points are two buttons in the Visualize tab's new Export / Report card. A source guard in the unit tests (`npm run test:export`, new `export-reporting` CI job) asserts neither module contains a network primitive, keeping the zero-upload promise intact. Why: users need to get their cleaned data and its validation verdict out of the browser as a shareable artifact without any upload path.
- Add a platform-aware capability registry so `js/app-shell/main.js` no longer statically imports every feature module regardless of runtime. Each capability in `capability-map.manifest.json` now declares a `platforms` list drawn from a closed set (`browser`, `desktop`, reserved `mobile`); most are `["browser", "desktop"]` (they behave identically in a plain browser and inside the Tauri desktop shell), and the Watch Folder is browser-only via a per-file `platformsByFile` override (its File System Access polling has no desktop-shell counterpart). A new loader `js/app-shell/capability-registry.js` reads that manifest at runtime (same-origin `fetch`, precached by `sw.js` and staged into the desktop bundle — no new network/upload path), detects browser vs. Tauri (`window.__TAURI__`), and dynamically `import()`s only the modules whose platform list includes the detected runtime, exposing `registry.get(name)`/`has`/`available`/`list`. `js/app-shell/main.js` migrates 13 representative feature modules onto the registry (a browser-only one, universals) while the rest keep working via their existing static imports; the remaining modules are tracked in a follow-up issue. Requesting a wrong-platform or unknown capability returns `undefined` with a clear `console.warn` (the runtime safety rail) rather than crashing — see the Watch Folder guard in `initWatchFolder`. The capability-map drift gate (`npm run test:capdrift`) now fails the build if any capability is missing a valid, non-empty `platforms` list, and `npm run test:capregistry` unit-tests the loader (both in the `capability-map-drift` CI job). Why: give each runtime only the modules that make sense for it, catch platform misclassification at merge time, and stop main.js from eagerly loading code a given runtime can't use.
- Add "Living Manifest" public-presence automation: one new workflow (`.github/workflows/living-manifest.yml`, on push to `main` + `workflow_dispatch`) that regenerates repo-facing artifacts from the same `capability-map.manifest.json` the drift gate already validates, plus git history — so the public face can never silently disagree with the gated source of truth. (1) A capability dashboard table auto-injected into `README.md` between `CAPABILITY_TABLE_START/END` markers (`.github/scripts/render-capability-dashboard.mjs`, `npm run docs:dashboard`). (2) A `docs/PROVENANCE_TIMELINE.md` timeline generated from `git log` (`.github/scripts/render-provenance-timeline.mjs`, `npm run docs:provenance`) — rendered as a markdown table rather than reusing the browser-only `js/runtimes-viz/visualize.js`, which needs the DOM/Plotly and can't run standalone in Node. (3) A wiki-gap detector (`.github/scripts/wiki-gap-detector.mjs`, `npm run docs:wiki-gap`) that opens a "Wiki page needed: <area>" issue for any capability area absent from `docs/wiki-coverage.json`, idempotent (skips titles that already exist) and capped per run. Pure-function logic is unit-tested (`npm run test:living-manifest`, new `living-manifest` CI job). The auto-commit uses `[skip ci]` to prevent a workflow loop, and the generators are no-ops when output is unchanged. Docs/metadata only — never touches `js/`, `index.html`, `css/`, `sw.js`, or `manifest.webmanifest`. Also set repo topics + a more specific description and tagged the `v1.0.0` release via `gh` (one-time settings, not workflow steps). Why: extend the manifest-as-source-of-truth discipline outward to the README, release history, and wiki backlog so they stay honest automatically.

- Add an optional Tauri v1 desktop shell (`src-tauri/`) that wraps the existing static site in a native window with no bundler/transpiler and no new copy of the app. Because Tauri v1 refuses a `distDir` containing `node_modules`/`src-tauri`, a tiny copy step (`scripts/stage-desktop-frontend.mjs`, wired via `beforeBuildCommand`/`beforeDevCommand`) stages the site's runtime assets (`index.html`, `manifest.webmanifest`, `sw.js`, `assets/`, `css/`, `js/`, `protocol/`) verbatim into a gitignored `src-tauri/dist/` that `distDir` points at — a plain file copy, so served bytes match the browser. The Rust side is the stock vanilla template (`src-tauri/src/main.rs` registers no commands) and the v1 allowlist is deny-by-default (`tauri.allowlist.all = false`), so the window has only what a browser tab has — the site's opt-in CDN runtimes and Databricks pull are ordinary webview requests and keep working. Adds `npm run tauri:dev`/`tauri:build`/`tauri:build:debug` (Tauri CLI via `npx`, so `package-lock.json` is untouched), a `tauri-smoke` CI job (`.github/workflows/job-tauri-smoke.yml`, debug build on `ubuntu-22.04` since Tauri v1 needs webkit2gtk-4.0) and `docs/desktop-shell.md`. The produced installers are explicitly NOT signed or notarized — the docs record what real signing would take (macOS notarization needs the ~US$99/yr Apple Developer Program; unsigned Windows binaries trip SmartScreen). Native SQLite, a filesystem bridge and mobile targets are intentionally out of scope. Why: give DATAGLOW a native desktop entry point without changing the app, adding a backend, or expanding its capability surface beyond a browser tab.
- Add a Problem Framer tab: a pre-analysis wizard that turns a vague business question ("sales feel off this quarter") into a specific, measurable analytical one before any querying. Intake textarea → four fixed SMART-style prompts (decision at stake, time window, audience, definition of done) → a deterministically restated analytical question → keyword/substring column suggestions matched against the loaded dataset's column names (skipped gracefully with a prompt to load data when none is loaded) → a one-page Markdown recap you can download or copy for a meeting note. Fully offline and deterministic — the prompt set is hand-authored, not model-generated, and no data leaves the browser. Pure logic in `js/problem-framing/problem-framer.js` (`npm run test:framer`, new `problem-framer` CI job); UI wiring in `js/app-shell/main.js`. Why: the biggest analysis mistakes are set before the first query — analyzing a fuzzy question well is still the wrong answer.

- Fix two documentation-accuracy issues. (1) Correct the overclaimed "nothing is fetched from a third party at runtime" statement in `AGENTS.md`, `README.md` and `package.json`: only DuckDB-WASM was actually vendored, while Plotly and SheetJS loaded from public CDNs on every page load and Pyodide/WebR/WebLLM load lazily. Took a hybrid fix — vendored Plotly.js 2.35.2 (MIT, `assets/plotly/`) and SheetJS/xlsx 0.18.5 (Apache-2.0, `assets/xlsx/`) locally since they load on every page and are single small files, made the Pyodide loader lazy in `js/runtimes-viz/python-runtime.js` (WebR already dynamically `import()`s its own), and reworded the docs so the split is honest: DuckDB+Plotly+SheetJS are self-hosted (page load fetches nothing third-party) while the large Pyodide/WebR/WebLLM runtimes remain CDN-loaded on demand. The separate, accurate "your data never leaves your machine" promise is untouched. (2) Replace the stale `🟢 Actively building — Gen 7` status line in `README.md` with a non-brittle pointer to this changelog as the source of truth. Why: successive stateless agents trust these docs, so an overstated or stale claim silently misleads every later session.

- Restructure the three files that kept colliding on parallel PRs into append-only zones, and split CI into per-job reusable workflows. `docs/CHANGELOG.md` (this `## Unreleased` section), `AGENTS.md` (the new *Foundations & capabilities* section), and `.github/workflows/test.yml` (the job list) now each carry an explicit append-only marker, so a new entry is a single-line insert at one fixed point and two PRs adding different entries no longer textually conflict. `.github/workflows/test.yml` is now a thin orchestrator that `uses:` one reusable workflow per job, each a top-level `.github/workflows/job-<name>.yml` file (each triggered via `on: workflow_call`; kept at the top level rather than a `jobs/` subdirectory because GitHub only resolves reusable workflows from the top level of `.github/workflows/`), so adding/changing a CI job touches its own small file instead of the shared 8-job YAML; also added `merge_group` as a trigger for future merge-queue compatibility. Check-run names change from a single segment (e.g. `SQL logic (native DuckDB)`) to two segments (e.g. `sql-logic / SQL logic (native DuckDB)`); no required status checks are currently configured on `main`, so nothing silently stops enforcing. Why: nearly every new CI/infra foundation had to edit all three files, making merge conflicts the norm whenever two foundations landed in parallel.
- Add Supply-Chain Install Hardening: a root `.npmrc` with `ignore-scripts=true` (disables dependency preinstall/install/postinstall lifecycle scripts, the most common supply-chain attack vector), a new `supply-chain-hardening` CI job, and `.github/scripts/check-lifecycle-scripts.mjs` (`npm run test:lifecycle-scripts`) that fails the build if any dependency declares a lifecycle script not on its explicit allowlist. The job also publishes a CycloneDX SBOM (`docs/sbom.json`) as a build artifact. Verified the DuckDB dependencies need no install scripts (so nothing rebuilds); only the dev-only `playwright-chromium` carries one, and it is allowlisted. Known limitation: `min-release-age=7` was intentionally omitted because it requires npm ≥ 11.10.0 and CI runs npm 10.8.2 — re-add once the toolchain upgrades. Why: a long-lived, dependency-light project should make install-time code execution a reviewed, versioned decision rather than something that can creep in silently.
- Add an AGENTS.md Context-Rot Detector (`.github/scripts/agents-md-drift.mjs`, `npm run test:agentsdrift`, new `agents-md-drift` CI job) that fails the build when `AGENTS.md` references a file path no longer in the tree or an npm script absent from `package.json`. Pure static analysis (regex extraction of backtick-quoted file-path- and npm-script-like tokens + filesystem/`package.json` lookups — no LLM, no network); wildcard globs like `test:*` are treated as descriptive and never failed on. Why: every stateless coding subagent reads and trusts `AGENTS.md` before working, so a stale reference silently misleads each later generation — this keeps the shared instructions honest. Ran green against the current `AGENTS.md` (9 file references, 0 stale), so no existing references needed fixing.
- Add a Capability-Map Drift Detector (`capability-map.manifest.json`, `.github/scripts/capability-drift.mjs`, `npm run test:capdrift`, new `capability-map-drift` CI job) that fails the build when the capability-map docs and the shipped `js/` code fall out of sync — an overclaim (a documented capability's backing file/symbol is gone), a dangling doc reference, or an underclaim (a shipped top-level `js/` module with no capability-map entry). Why: the capability map is only useful if it stays honest, and nothing enforced that until now. Also fixes one real underclaim found while authoring the manifest — the shipped Devil's Advocate mode (`js/analysis-robustness/devils-advocate.js`) was missing from the map and is now documented.
- Add a Dependency Freshness Ledger (`.github/scripts/dependency-freshness.mjs`, `npm run test:deps-freshness`, new `dependency-freshness` CI job) that reads `package.json`/`package-lock.json`, queries the npm registry (`npm outdated`/`npm audit`), and regenerates `docs/dependency-freshness-ledger.md`; it FAILS CI when a dependency is more than 2 majors behind or a high/critical advisory is found, and only warns on minor/patch drift. Why: a long-running side project silently accumulates stale, potentially vulnerable dependencies — this makes that drift a visible, versioned artifact and gates the worst cases.
- Add a proof-of-concept Databricks Direct-Connect: optional BYO-token, browser-direct, read-only pull from a user's own Databricks SQL warehouse into local DuckDB, reusing the file-import ingest path. Why: validate whether users can bring warehouse data into DATAGLOW's local engine without a server or persisted credentials (`js/app-shell/databricks-connect.js`, `docs/databricks-connect.md`).
- Wire the `test:databricks` suite into CI as its own `databricks-connect` job so it runs on every push/PR. Follow-up fix: the Databricks Direct-Connect PR added the script and test but never ran them in GitHub Actions (`.github/workflows/test.yml`).
- Add a Golden Regression Suite (`test/golden/`, `npm run test:golden`, new CI job) that snapshot-tests the core deterministic operations — SQL cleaners, the validation-layer orchestrator, cross-column/bounds checkers, and the calibrated-grade roll-up — against versioned fixtures; why: give the fast-moving, agent-authored feature stream a safety net so adding a feature can't silently change existing output.
- Add a progressive-disclosure capability map (`docs/capability-map.md` + `docs/capability-map/`) that maps every `js/` module to a feature area (#34).
- Document the plan-before-code and changelog-on-completion conventions in a new root `AGENTS.md`, and start this changelog (#34).

## 2026-07-08

- Add tech-debt tracker plus a weekly read-only entropy-reduction scan (#33).
- Add the Unified Signal Layer, a shared in-memory coordination store for the on-device analysis modules (#32).

## 2026-07-07

- Add Expected Value Ranges: informational numeric-column trend bands alongside the drift layer (#31).
- Add Federated Fingerprint Learning (Phase 1): opt-in, off by default, privacy-preserving (#30).
- Add Forecast-Based Drift Alerting using Holt's exponential smoothing to extend the distributional-drift layer (#29).
- Add an in-browser small-model (WebLLM / Qwen2.5-1.5B) mode to the Story tab (#28).
- Add Adaptive Layer Prioritization: on-device learned ordering of the Validate layers (#27).
- Add Predictive Anomaly Scoring: a holistic kNN/Gower row-level outlier detector (#26).
- Add Self-Learning Validation Rules: on-device logistic regression over the user's own corrections (#25).
- Fix inconsistent validation-layer counts across docs and UI (verified: 20 layers) (#24).
- Rename the "ZK-proof" feature to "selective-disclosure provenance proof" to correct a cryptographic overclaim (#23).
- Correct unverified dataset-scale and synthetic-fidelity claims in the docs (#22).

<!-- CI trigger: verifying living-manifest-automation checks -->
